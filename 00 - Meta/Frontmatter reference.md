# Frontmatter reference

Moved. Everything that was here — the base header, the quality scale, the value
formatting rules and the retired-field list — now lives in
`00 - Meta/YAML and Tags.md`, alongside the type, era and tag vocabularies.

That file is the single entry point for frontmatter. This stub exists only so
older links do not dead-end.
